@AGENTS.md

初心者サポートでは、`.claude/skills/machiba-beginner-support/SKILL.md`を入口にして、そこから案内される正本を読みます。参加者の対象プロジェクトを開いたスレッドで、GitHubセッションIssueの作成・再開・read-backを行います。

このリポジトリ自体を編集するときは、`CONTRIBUTING.md`を読み、`claude/`で始まる専用ブランチと専用worktreeを使います。`main`へ直接pushせず、Draft Pull Request、`npm test`、レビュー、squash mergeの順で進めます。ほかのエージェントの変更を削除、上書き、巻き戻しません。
