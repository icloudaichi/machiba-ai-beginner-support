# 安全に使うために

このリポジトリは公開されています。教材、質問、サンプルへ次の情報を入れないでください。

- パスワード
- メールやSMSの認証コード
- GitHub・Cloudflareなどのアクセストークンや秘密鍵
- カード情報
- 本物の顧客情報や未公開の個人情報
- 本番サービスの秘密の設定値
- 接続診断に表示されるユーザー名、メールアドレス、アカウントID
- AIとの会話全文、思考過程、コマンドやエラーの生出力
- 端末名、ローカルユーザー名、フォルダの絶対パス

`.env`、`.env.local`、`.wrangler/`などのローカル情報はGitへ追加しません。公開サンプルには架空データだけを使います。

GitHub・Cloudflareの接続診断は、出力をチャットへ貼る方法では行いません。AIから確認する場合は標準出力と標準エラーを捨て、終了コードだけを使います。

## AI相談セッションの記録

参加者ログの標準記録先は、参加者自身の非公開アプリリポジトリです。この公開教材リポジトリには記録しません。

Issueへ書ける内容は、成功、失敗の分類、`blocked`、現在地、次の一手、remoteで確認済みのcommit SHAなど、短い構造化情報に限ります。書き込み後は同じIssueをread-backし、確認できなければ「未反映」と扱います。

セッション開始・再開時に対象GitHub repoのcanonical名と不変IDをローカル状態へ結び付けます。以降の操作でoriginまたはGitHub repo IDが変わった場合は、同じセッションや公開許可を別repoへ持ち越さず停止します。

公開リポジトリへのセッション記録は初期状態で拒否します。本人が公開範囲を理解して明示的に許可し、自動記録ツールへ`--allow-public`を渡した場合だけ例外とします。公開許可は、秘密情報や個人情報を書いてよいという許可ではありません。

標準開始プロンプトの限定承認は、セッションIssueの作成・再開、allowlist項目の追記、read-back、目的完了時のIssue closeだけに適用されます。コード変更、merge、Cloudflare公開、D1作成、外部送信は許可しません。

## 誤って共有した場合

1. 値を別の場所へコピーしたり、会話で復唱したりしないでください。
2. 対象サービスで、その値を失効・変更・再発行してください。
3. 新しい値もチャットや公開リポジトリへ貼らないでください。
4. 必要に応じて、信頼できる担当者またはサービスの公式サポートへ相談してください。

投稿を削除できても、それだけで安全になったとは限りません。パスワードやトークンは必ず変更・失効まで確認してください。

セキュリティ上の問題を報告するときは、秘密情報や再現用の認証情報を公開の投稿へ書かず、リポジトリ管理者へ非公開の方法で知らせてください。公開教材の改善Issue Formはセキュリティ事故の報告先ではありません。
