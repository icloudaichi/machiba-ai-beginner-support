# 安全に使うために

このリポジトリは公開されています。教材、質問、サンプルへ次の情報を入れないでください。

- パスワード
- メールやSMSの認証コード
- GitHub・Cloudflareなどのアクセストークンや秘密鍵
- カード情報
- 本物の顧客情報や、保存了承済みニックネーム以外の未公開個人情報
- 本番サービスの秘密の設定値
- 接続診断に表示されるユーザー名、メールアドレス、アカウントID
- AIとの会話全文、思考過程、コマンドやエラーの生出力
- 端末名、ローカルユーザー名、フォルダの絶対パス

`.env`、`.env.local`、`.wrangler/`などのローカル情報はGitへ追加しません。公開サンプルには架空データだけを使います。

GitHub・Cloudflareの接続診断は、出力をチャットへ貼る方法では行いません。AIから確認する場合は標準出力と標準エラーを捨て、終了コードだけを使います。

## AI相談セッションの記録

参加者ログの標準記録先は、参加者自身の非公開アプリリポジトリです。この公開教材リポジトリには記録しません。

サイトの開始時に本人が選んだニックネームは、説明と確認後、この端末の進捗へ保存できます。相談履歴へ保存するのは、private Issueとcollaboratorに見えることを別に説明し、Issue保存を明確に了承したニックネームだけです。日本語でも構いません。端末保存の了承をIssue保存へ流用しません。本名、申込時の氏名、メールアドレス、電話番号、住所、GitHubユーザー名、アカウントIDを表示名として取得・転記しません。

GitHubのrepo名はニックネームとは別です。`machiba-ai-app`、同名なら`machiba-ai-app-2`のような非個人のASCII名を使い、本名やニックネームをrepo名へ入れません。申込時の氏名とニックネーム、private repoの対応は、公開リポジトリではなく閲覧制限した運営台帳だけで管理します。

Issueへ書ける内容は、保存了承済みニックネーム、相談内容、背景、試したこと、失敗の安全な要約、解決方法、学び、次の一手、remoteで確認済みのcommit SHAなど、短い構造化情報に限ります。書き込み後は同じIssueをread-backし、確認できなければ「未反映」と扱います。

セッション開始・再開時に対象GitHub repoのcanonical名と不変IDをローカル状態へ結び付けます。以降の操作でoriginまたはGitHub repo IDが変わった場合は、同じセッションや公開許可を別repoへ持ち越さず停止します。

ニックネームと詳細な相談履歴は公開リポジトリへ記録しません。対象がpublicなら、参加者自身のprivate app repoを準備してから相談履歴を開始します。

標準開始プロンプトの限定承認は、本人が選び保存を了承したニックネーム、allowlistの相談履歴、read-back済みの成果物提出結果をprivate repoのセッションIssueへ作成・再開・追記し、read-backし、目的完了時にIssueをcloseする範囲だけに適用されます。コード変更、collaborator招待、merge、Cloudflare公開、D1作成、Google Driveへのアップロード、外部送信は許可しません。

## Google Driveへの成果物提出

2026年8月23日の提出先は、リンク所持者が閲覧・追加できる共有フォルダです。提出ファイル名の名前は、ほかの参加者やリンク所持者にも見えます。private Issueのニックネームを同じ値にすると推測せず、同じ名前を使うか、提出用に別の名前を使うか、共有ファイル名へ使ってよいかを別に確認します。本名は不要です。

標準はブラウザからの手動アップロードです。AI接続は本人が希望した場合の発展とし、Google公式OAuthまたは公式コネクタを一操作ずつ案内します。権限画面ごとに目的と影響を説明して確認を待ち、APIキー、認証コード、トークンをチャットへ貼らせません。同じ箇所で3回または約7分止まったらブラウザ提出へ戻ります。

接続承認はアップロード承認ではありません。書き込み直前に対象ファイル、提出先、共有範囲への影響を示し、明確な了承を得ます。共有権限は変更せず、自分のファイルの新規追加だけを行います。ほかの参加者のファイルを開く、更新する、移動する、改名する、削除する操作は禁止です。

提出ZIPへ`.env`、秘密鍵、トークン、認証コード、`node_modules`、`.wrangler`、`.git`のローカル状態、個人・顧客データを含めません。アップロード後は一覧またはメタデータをread-backし、指定フォルダの親と完全一致するファイル名を確認できた場合だけDrive提出済みとします。個別URLは確認できた場合だけ記録し、確認できなければ省略します。

保存了承済みニックネーム、別途了承された提出ファイル名、個別Drive URL、経路、確認結果、次の一手は、参加者自身のprivate app repoにある本人Issueへだけ記録します。Drive確認済みとGitHub Issue確認済みを別々に報告し、Drive確認済み・Issue未反映の場合は再アップロードせずIssue記録だけを再開します。Issueには生の提出フォルダIDを残しません。公開教材repoや公開Issueへ提出記録を集約しません。

提出先の直接URLと書き込み権限は公開教材から見えます。2026年8月23日18:00の講座終了後、運営は新規追加を止めるため共有権限を見直します。権限変更はAIへ自動実行させず、運営が対象と影響を確認して行います。

## 運営collaborator

和佐さん・下山さんなどの運営がニックネーム別の進捗を見るには、参加者が自分のprivate app repoへ、対象となる運営GitHubアカウントをcollaboratorとして招待する必要があります。申込時の氏名との対応は閲覧制限した運営台帳でのみ行います。

招待すると、相談Issueだけでなく、repo内のコードやほかのIssueも、設定された権限の範囲で運営から見えることを先に説明します。招待は相談履歴の限定承認に含めません。正しいGitHubユーザー名、権限、対象repoを本人へ提示し、明示了承後だけ行います。

参加者が招待しない場合、個人ログを公開教材repoや公開Issueへコピーして代替しません。

## 誤って共有した場合

1. 値を別の場所へコピーしたり、会話で復唱したりしないでください。
2. 対象サービスで、その値を失効・変更・再発行してください。
3. 新しい値もチャットや公開リポジトリへ貼らないでください。
4. 必要に応じて、信頼できる担当者またはサービスの公式サポートへ相談してください。

投稿を削除できても、それだけで安全になったとは限りません。パスワードやトークンは必ず変更・失効まで確認してください。

セキュリティ上の問題を報告するときは、秘密情報や再現用の認証情報を公開の投稿へ書かず、リポジトリ管理者へ非公開の方法で知らせてください。公開教材の改善Issue Formはセキュリティ事故の報告先ではありません。
